#include "monty.h"

/**
 * nop_monty - it does nothing
 * @stack: to do nothing in
 * @line_number: to do nothing with
 */
void nop_monty(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
